---
import '../styles/site.css';

const {
  title = 'sshoosh',
  description = 'A self-hosted SSH/TUI workspace chat.',
  current = 'home',
} = Astro.props;

const base = import.meta.env.BASE_URL;
const href = (path: string) => `${base}${path}`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <script is:inline>
      (() => {
        const stored = localStorage.getItem('sshoosh-theme');
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        document.documentElement.dataset.theme = stored || (prefersDark ? 'dark' : 'light');
      })();
    </script>
    <title>{title}</title>
    <meta name="description" content={description} />
  </head>
  <body>
    <div class="site-frame">
      <header class="topline">
        <a class="brand" href={href('')}>sshoosh</a>
        <nav aria-label="Primary navigation">
          <a href={href('')} aria-current={current === 'home' ? 'page' : undefined}>landing</a>
          <a href={href('docs/')} aria-current={current === 'docs' ? 'page' : undefined}>docs</a>
          <button class="theme-toggle" type="button" data-theme-toggle>theme</button>
        </nav>
      </header>
      <slot />
    </div>
    <script is:inline>
      (() => {
        const button = document.querySelector('[data-theme-toggle]');
        if (!button) return;

        const sync = () => {
          const theme = document.documentElement.dataset.theme || 'light';
          button.textContent = theme === 'dark' ? 'light' : 'dark';
          button.setAttribute('aria-label', `Switch to ${button.textContent} mode`);
        };

        button.addEventListener('click', () => {
          const next = document.documentElement.dataset.theme === 'dark' ? 'light' : 'dark';
          document.documentElement.dataset.theme = next;
          localStorage.setItem('sshoosh-theme', next);
          sync();
        });

        sync();
      })();
    </script>
  </body>
</html>
